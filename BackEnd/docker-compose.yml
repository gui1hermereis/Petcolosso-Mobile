version: '3'

services:
  csi-api:
    container_name: app-petcolosso-api
    hostname: petcolosso-api
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4001:8080"        
    command: bash -c "npm run start"


